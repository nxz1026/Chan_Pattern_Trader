# dev 依赖(顶层声明),由 pip-compile 生成 requirements-dev.txt 锁版本
#
# 升级流程:
#   1. 改这里(或 pyproject.toml [project.optional-dependencies].dev)
#   2. pip-compile requirements-dev.in -o requirements-dev.txt
#   3. CI 与本地统一用 requirements-dev.txt

ruff>=0.6.0
mypy>=1.10
pytest>=8.0
pytest-cov>=5.0
pre-commit>=3.7
import-linter>=2.0
types-requests
pip-tools>=7.0
# 死代码扫描。2026-09-25 排查前它**不在锁文件里**：CI 单独 `pip install
# 'vulture==2.14'`、pre-commit 完全没装，于是版本不受锁文件约束、本地也不一定有。
# 锁进来之后 CI 与 pre-commit 用同一份。
vulture==2.14
