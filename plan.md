# Cryptocurrency Trading（CPT）开发计划

## 0. 项目定位

CPT（Cryptocurrency Trading）是一个面向虚拟货币行情结构评测的软件，第一阶段聚焦：

- Binance BTCUSDT USDT 本位永续合约公开行情；
- 5分钟、30分钟、4小时、日线多级别结构；
- 缠论基础结构：缠论K线、分型、笔、中枢、走势类型、背驰和买卖点；首版不实现线段；
- 以走势类型为基础构件的级别递归映射；
- 历史回放、结构可视化和信号评测；
- 第一阶段不自动下单。

`chanlun-pro` 作为候选参考实现，不作为 CPT 的最终规则标准。TradingView/Pine 结果只作为可视化交叉校验。

## 1. 开发原则

1. 先定义规则，再实现代码。
2. 对 `chanlun-pro` 已明确且与 CPT 不冲突的基础规则，直接参考并固定其实现口径；不重复设计。
3. 结构算法优先参考本地固定版本 `references/chanlun-pro`（`78ffa470`），包括中枢 `level` 与 `zs_wzgx`；不直接复制其线段和多周期展示逻辑。
4. 先使用人工构造数据进行单元测试，再接入真实行情。
5. 所有实时信号区分“候选、收盘确认、最终确认”。
6. 明确记录结构重构和信号撤销，不把未来数据带入历史评测。
7. 多周期“数据合成”和“结构递归”分开实现，不能混为一谈。
8. 每个阶段都要有可重复的命令、测试和验收结果。
9. 默认只读公开行情，不接入交易下单权限。

## 2. 分阶段计划

### 阶段 A：规则规格冻结

目标：建立 CPT 自己的规则规格，不直接继承第三方默认值。

需要讨论并冻结：

- K线包含关系及上涨/下跌方向处理；
- 分型构成、确认时点和区间取值；
- 笔类型及成立条件；
- 线段特征序列和笔破坏规则；
- 中枢元素、重叠区间和结束条件；
- 走势类型的构成与完成条件；
- 5分钟→30分钟→4小时→日线的结构递归方式；
- 一买的必要条件与确认时点；
- 历史结构是否允许重构；
- 实时信号候选、收盘确认、最终确认的定义。

交付物：

- `docs/rules.md`：正式规则；当前已落地基础继承规则和 CPT 专属五项约定；
- `docs/rules-decision-log.md`：每项规则的讨论和决策记录；
- 人工构造案例及预期结果。

验收标准：每条核心规则都能写成输入、处理、输出和确认时点，不能只使用“符合缠论”这类不可执行描述。

### 阶段 B：项目与算法基线审计

目标：审计 `chanlun-pro`、`chanlun.py`、Pine 参考脚本与 CPT 规则的差异。

工作内容：

- 固定第三方仓库版本和来源链接；
- 记录许可证和可复用范围；
- 对比分型、笔、线段、中枢、买卖点；
- 记录加密代码、不可复核逻辑和默认配置风险；
- 确认是否存在未来函数或历史重算影响。

交付物：

- `docs/reference-audit.md`；
- 差异矩阵；
- 是否复用、改写或自研的决策。

### 阶段 C：最小数据模型与确定性核心

目标：先实现不依赖交易所的纯结构计算核心。

建议模块：

- 标准 OHLCV 数据模型；
- 缠论K线合并；
- 分型；
- 笔；
- 线段；
- 中枢；
- 走势类型；
- 背驰与买卖点；
- 结构状态和事件日志。

要求：

- 输入相同数据必须产生相同结果；
- 每个结构保留来源K线、成立时间、确认时间和失效时间；
- 不在核心层隐藏未来数据；
- 先支持批量计算，再设计逐根K线增量计算。

交付物：核心包、类型定义、单元测试、结构事件样例。

### 阶段 D：规则驱动的多级别递归

目标：实现 CPT 自己的级别递归，不把普通K线周期聚合误认为结构递归。

初始链路：

```text
5分钟笔
→ 5分钟走势类型
→ 30分钟线段
→ 30分钟走势类型
→ 4小时线段
→ 4小时走势类型
→ 日线线段
```

要求：

- 明确每一级输入只能使用哪些已确认的低级别结构；
- 记录低级别结构到高级别结构的映射；
- 低级别重构时，高级别如何回滚或保持；
- 支持实时增量和历史重放两种模式；
- 不能使用尚未产生的未来K线确认历史信号。

交付物：递归引擎、级别映射、边界案例测试、重构事件测试。

### 阶段 E：行情数据与存储

目标：接入公开加密货币行情并支持可复现回放。

第一阶段范围：

- Binance USDT 永续公开K线；
- BTCUSDT、ETHUSDT 作为首批标的；
- 5m、30m、4h、1d；
- UTC 时间统一；
- 原始数据与标准化数据分层保存；
- 去重、断点续传、缺口检测；
- 数据版本和来源记录。

交付物：数据采集器、数据校验器、离线数据集、回放接口。

### 阶段 F：评测、回放与可视化

目标：让用户能看到结构如何形成、确认、重构和失效。

功能：

- K线与缠论结构图；
- 多级别同步查看；
- 单根推进回放；
- 候选/确认/失效信号显示；
- 一买案例评测；
- 结构差异对比；
- JSON/API 输出。

第一阶段不做自动交易，不把图表信号直接转换为下单指令。

### 阶段 G：回测与质量验收

目标：在无未来函数的前提下评估规则表现。

验收内容：

- 人工案例逐条通过；
- 批量历史回放结果可复现；
- 实时模式与离线回放结果一致；
- 信号出现、确认、撤销时间可追溯；
- 结构重构不产生隐性未来函数；
- 与 `chanlun-pro` 和 Pine 结果的差异可解释；
- 数据缺口、重复K线、未收盘K线有明确处理。

交付物：测试报告、差异报告、已知限制清单。

## 3. 当前讨论顺序

我们按以下顺序逐步讨论，不提前写实现代码：

1. 先冻结阶段 A 的规则规格；
2. 再决定哪些规则复用 `chanlun-pro`，哪些规则 CPT 自己实现；
3. 再确定技术栈和目录结构；
4. 再实现最小结构核心；
5. 最后接行情、回放和界面。

## 4. 当前未决问题

- [ ] CPT 使用哪种K线包含关系规则？
- [ ] CPT 使用老笔、新笔、简单笔还是自定义笔？
- [ ] 线段是否采用特征序列标准规则？
- [ ] 中枢采用标准、段内、方向还是分类中枢？
- [ ] 走势类型完成的必要条件是什么？
- [ ] 5分钟到30分钟采用“结构映射”还是“周期K线重算”？
- [ ] 一买是否必须满足两个严格同级下跌中枢加趋势背驰？
- [ ] 实时信号是否只在K线收盘后计入正式评测？
- [ ] 历史已确认结构是否永久冻结？
- [ ] 首个版本是否只支持 Binance、BTCUSDT、ETHUSDT？
